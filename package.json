{
  "scripts": {
    "setup": "rimraf database.db && sqlite3 database.db < setup.sql",
    "build": "rimraf out && mkdir out && tsc",
    "start": "cd out && node server.js",
    "watch": "npm run build && tsc-watch --onSuccess \"npm run start\"",
    "build:deploy": "rimraf out && mkdir out && tsc && (cd front && npm run build)",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --runInBand --rootDir=src",
    "test-data": "npm run setup && sqlite3 database.db < insert.sql"
  },
  "type": "module",
  "dependencies": {
    "@types/cookie-parser": "^1.4.3",
    "argon2": "^0.30.3",
    "cookie-parser": "^1.4.6",
    "express": "^4.18.2",
    "pm2": "^5.2.2",
    "sqlite": "^4.1.2",
    "sqlite3": "^5.1.4",
    "uuid": "^9.0.0",
    "zod": "^3.20.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "@types/jest": "^29.2.5",
    "@types/node": "^18.11.18",
    "@types/uuid": "^9.0.1",
    "axios": "^1.2.2",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "tsc-watch": "^6.0.0",
    "typescript": "^4.9.4"
  }
}
